# Copy this file to .env.local for local development (Vite proxy handles /api → localhost:8000).
# Set VITE_API_BASE_URL in Vercel Environment Variables for production.

# Leave unset in development; Vite proxy forwards /api/* to http://127.0.0.1:8000
# VITE_API_BASE_URL=

# Production (Vercel env var):
# VITE_API_BASE_URL=https://SmartSearchapi.culturemind.org
